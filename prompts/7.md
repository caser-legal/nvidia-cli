Clean-build-install

(Never install on the simulator; always on the iPhone - 00008110-00061CD921A3A01E) (Skip if the phone is locked!)

&&

1. Check gh repo list to find your existing repo (make one if it doesn't exist, run command, auth is already provided)
2. Add the remote
3. Push

1. gh repo list | grep -i [project] → find or create repo
2. git remote set-url origin https://... → add remote (HTTPS)
3. git push → push it

--End of Prompt @7
