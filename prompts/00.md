Your goal is to deploy subagent1 and subagent2 to do 33% of the total work. You are the main agent to ensure all of this is complete. Taking on the remaining 34%.  You will then check their work too, just like a teacher would.


Understood? Ready for the "issues" and then "claims of fixes" you and your 2 subagents are tasked with ensuring are true and complete? 

Tool Permission for all agents (including you)

Built-in context7 (MCP) and nvidia-cli (MCP) commands are trusted.

- query-docs, resolve-library-id, bash, code_documentation, compare_mockup, deduplicate_sources, documentation_specialist, dory_agent, entity_memory, extend_report, file_read, file_write, flywheel_create_dataset, flywheel_export, flywheel_log, flywheel_stats, get_project, github_analyzer, github_file_reader, google_search, health_check, ios_ui_review, local_docs_search, memory, mermaid_generator, parallel_search, quality_reviewer, quick_diagram, rag_clear, rag_ingest, rag_query, rag_research, rag_search, rag_stats, rag_update, rag_validate, reflection, report_compiler, report_extender, report_planner, report_writer, search_specialist, section_author, set_project, think, unified_memory, vision_analyze

DO NOT FORGET TO RUN A RUNTIME BEHAVIOR AUDIT - you are REQUIRED to actually use the app and test real functionality, not just checking if code compiles.

DO NOT VIOLATE MANDATORY RULES. The core principle from the prompts:

│ **"Ask: 'What breaks when I actually USE the app?' not just 'Does the code look 
correct?'"**

Use parallel_search to research [TOPIC].

Read the results.

Based on what you learned, run parallel_search AGAIN with refined queries.

Read those results.

Run parallel_search ONE MORE TIME to fill any gaps.

Then summarize what you found.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


## Example:

Use parallel_search to research "how to use Kiro delegate feature".

Read the results.

Based on what you learned, run parallel_search AGAIN with refined queries.

Read those results.

Run parallel_search ONE MORE TIME to fill any gaps.

Then summarize what you found.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


## Why it works:

1. "parallel_search" - tells me the exact tool
2. "Read the results" - forces me to actually process before moving on
3. "AGAIN with refined queries" - makes me iterate based on findings
4. "ONE MORE TIME" - ensures thorough coverage
5. "summarize" - gives you the answer at the end

--End of Prompt @0)